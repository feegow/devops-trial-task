apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: available-schedules-python
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: available-schedules-python
  minReplicas: 1
  maxReplicas: 2
  behavior: { }  # falta stabilizationWindow; candidato pode melhorar
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 30